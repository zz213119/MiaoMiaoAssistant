diff --git a/app/src/main/java/com/example/u7e5f3218e9/CatConfig.java b/app/src/main/java/com/example/u7e5f3218e9/CatConfig.java
index aa640c1..cc2d86c 100644
--- a/app/src/main/java/com/example/u7e5f3218e9/CatConfig.java
+++ b/app/src/main/java/com/example/u7e5f3218e9/CatConfig.java
@@ -26,6 +26,9 @@ public class CatConfig {
     public static final String KEY_GLOBAL_MODE = "global_mode";
     public static final String KEY_ENABLE_DEBUG_LOG = "enable_debug_log";
     public static final String KEY_CUSTOM_PACKAGES = "custom_packages";
+    // 切到后台时是否主动调用 finishAndRemoveTask() 强制清除最近任务记录，
+    // 比静态的 excludeFromRecents 更强，但代价是每次都相当于"全新打开"，不能恢复到上次的页面
+    public static final String KEY_AGGRESSIVE_HIDE_RECENTS = "aggressive_hide_recents";
 
     public static final String PKG_QQ = "com.tencent.mobileqq";
     public static final String PKG_QQ_I = "com.tencent.mobileqqi";
@@ -67,6 +70,9 @@ public class CatConfig {
     // 关闭后能省一点点性能开销，也避免日志缓冲区被高频诊断信息挤满。
     public boolean enableDebugLog = false;
     public String[] customPackages = new String[0];
+    // 默认关闭：开启后每次切后台都会清掉任务记录，无法"秒回"到上次页面，
+    // 是否要这种更强力的隐藏效果交给用户自己决定
+    public boolean aggressiveHideRecents = false;
 
     /**
      * 判断某个包名当前是否处于处理范围内。
@@ -158,6 +164,7 @@ public class CatConfig {
         cfg.enableKuaishou = sp.getBoolean(KEY_APP_KUAISHOU, false);
         cfg.globalMode = sp.getBoolean(KEY_GLOBAL_MODE, false);
         cfg.enableDebugLog = sp.getBoolean(KEY_ENABLE_DEBUG_LOG, false);
+        cfg.aggressiveHideRecents = sp.getBoolean(KEY_AGGRESSIVE_HIDE_RECENTS, false);
         String customPkgStr = sp.getString(KEY_CUSTOM_PACKAGES, "");
         if (customPkgStr != null && !customPkgStr.trim().isEmpty()) {
             List<String> pkgList = new ArrayList<>();
@@ -215,6 +222,7 @@ public class CatConfig {
         ed.putBoolean(KEY_APP_KUAISHOU, this.enableKuaishou);
         ed.putBoolean(KEY_GLOBAL_MODE, this.globalMode);
         ed.putBoolean(KEY_ENABLE_DEBUG_LOG, this.enableDebugLog);
+        ed.putBoolean(KEY_AGGRESSIVE_HIDE_RECENTS, this.aggressiveHideRecents);
         ed.putString(KEY_CUSTOM_PACKAGES, join(this.customPackages, "\n"));
         ed.apply();
     }
diff --git a/app/src/main/java/com/example/u7e5f3218e9/MainActivity.java b/app/src/main/java/com/example/u7e5f3218e9/MainActivity.java
index 4b477c9..6a7bc9d 100644
--- a/app/src/main/java/com/example/u7e5f3218e9/MainActivity.java
+++ b/app/src/main/java/com/example/u7e5f3218e9/MainActivity.java
@@ -45,6 +45,7 @@ public class MainActivity extends Activity {
     private Button closeServiceButton;
     private CheckBox cbDebugLog;
     private CheckBox cbHideIcon;
+    private CheckBox cbAggressiveHideRecents;
     // 桌面图标入口的组件名，对应 Manifest 里的 <activity-alias android:name=".LauncherAlias">
     private static final String LAUNCHER_ALIAS_CLASS = "com.example.u7e5f3218e9.LauncherAlias";
 
@@ -57,6 +58,20 @@ public class MainActivity extends Activity {
     private EditText etCustomPackages;
     private LinearLayout appCheckRow;
 
+    /**
+     * 用户按 Home 键 / 切到其他应用时触发（不含来电、通知栏下拉等系统打断）。
+     * 开启"强力隐藏最近任务"时，主动把这次任务从最近任务列表里摘掉，
+     * 比静态的 android:excludeFromRecents 更硬、兼容性更好。
+     * 只影响这个 Activity 实例本身，无障碍服务是独立组件，不受影响，继续在后台跑。
+     */
+    @Override
+    protected void onUserLeaveHint() {
+        super.onUserLeaveHint();
+        if (this.config != null && this.config.aggressiveHideRecents) {
+            finishAndRemoveTask();
+        }
+    }
+
     @Override
     protected void onCreate(Bundle savedInstanceState) {
         super.onCreate(savedInstanceState);
@@ -223,12 +238,18 @@ public class MainActivity extends Activity {
             }
         });
 
-        TextView recentsHint = new TextView(this);
-        recentsHint.setText("已固定：本应用不会出现在\"最近任务\"列表里（此项无需开关，服务不受影响）");
-        recentsHint.setTextSize(11.0f);
-        recentsHint.setTextColor(Color.rgb(161, 136, 127));
-        recentsHint.setPadding(0, 0, 0, 8);
-        root.addView(recentsHint);
+        this.cbAggressiveHideRecents = addCheckbox(root, "强力隐藏最近任务（切后台自动清任务记录）",
+                "开启后每次切到后台都会主动清掉这个界面在最近任务里的记录，"
+                        + "比系统的\"excludeFromRecents\"更强、兼容性更好，适合本机 excludeFromRecents 不生效的情况。"
+                        + "代价：无法\"秒回\"到刚才那个页面，下次都相当于重新打开。无障碍服务不受影响，一直在后台跑。",
+                this.config.aggressiveHideRecents);
+        this.cbAggressiveHideRecents.setOnCheckedChangeListener(new CompoundButton.OnCheckedChangeListener() {
+            @Override
+            public void onCheckedChanged(CompoundButton buttonView, boolean isChecked) {
+                MainActivity.this.config.aggressiveHideRecents = isChecked;
+                MainActivity.this.config.save(MainActivity.this);
+            }
+        });
 
         TextView batteryHint = new TextView(this);
         batteryHint.setText("重要：图标/最近任务只是\"看不看得见\"，不代表系统不会清理后台。"
diff --git a/battery-perms.patch b/battery-perms.patch
deleted file mode 100644
index fbf0679..0000000
--- a/battery-perms.patch
+++ /dev/null
@@ -1,142 +0,0 @@
-diff --git a/app/src/main/AndroidManifest.xml b/app/src/main/AndroidManifest.xml
-index 454c60b..48c02a3 100644
---- a/app/src/main/AndroidManifest.xml
-+++ b/app/src/main/AndroidManifest.xml
-@@ -9,6 +9,8 @@
-     <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
-     <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
-     <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
-+    <!-- 用于弹出"忽略电池优化"的系统标准授权框，避免后台被省电策略杀掉 -->
-+    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
- 
-     <application
-         android:allowBackup="true"
-diff --git a/app/src/main/java/com/example/u7e5f3218e9/MainActivity.java b/app/src/main/java/com/example/u7e5f3218e9/MainActivity.java
-index a4f8073..4b477c9 100644
---- a/app/src/main/java/com/example/u7e5f3218e9/MainActivity.java
-+++ b/app/src/main/java/com/example/u7e5f3218e9/MainActivity.java
-@@ -9,6 +9,9 @@ import android.content.ComponentName;
- import android.content.DialogInterface;
- import android.content.Intent;
- import android.content.pm.PackageManager;
-+import android.net.Uri;
-+import android.os.PowerManager;
-+import android.provider.Settings;
- import android.graphics.Color;
- import android.graphics.Typeface;
- import android.os.Bundle;
-@@ -226,6 +229,50 @@ public class MainActivity extends Activity {
-         recentsHint.setTextColor(Color.rgb(161, 136, 127));
-         recentsHint.setPadding(0, 0, 0, 8);
-         root.addView(recentsHint);
-+
-+        TextView batteryHint = new TextView(this);
-+        batteryHint.setText("重要：图标/最近任务只是\"看不看得见\"，不代表系统不会清理后台。"
-+                + "划掉最近任务或系统省电策略仍可能杀掉无障碍服务进程，"
-+                + "务必点下面两个按钮去系统里放行，才能保证一直在跑：");
-+        batteryHint.setTextSize(11.0f);
-+        batteryHint.setTextColor(Color.rgb(198, 40, 40));
-+        batteryHint.setPadding(0, 4, 0, 8);
-+        root.addView(batteryHint);
-+
-+        Button batteryOptButton = new Button(this);
-+        batteryOptButton.setText("关闭电池优化（系统标准弹窗）");
-+        batteryOptButton.setTextSize(14.0f);
-+        LinearLayout.LayoutParams battBtnLp = new LinearLayout.LayoutParams(-1, -2);
-+        battBtnLp.setMargins(0, 4, 0, 4);
-+        batteryOptButton.setLayoutParams(battBtnLp);
-+        batteryOptButton.setOnClickListener(new View.OnClickListener() {
-+            @Override
-+            public void onClick(View v) {
-+                MainActivity.this.requestIgnoreBatteryOptimizations();
-+            }
-+        });
-+        root.addView(batteryOptButton);
-+
-+        Button vivoBgButton = new Button(this);
-+        vivoBgButton.setText("打开 vivo/iQOO 后台高耗电权限页");
-+        vivoBgButton.setTextSize(14.0f);
-+        LinearLayout.LayoutParams vivoBtnLp = new LinearLayout.LayoutParams(-1, -2);
-+        vivoBtnLp.setMargins(0, 4, 0, 4);
-+        vivoBgButton.setLayoutParams(vivoBtnLp);
-+        vivoBgButton.setOnClickListener(new View.OnClickListener() {
-+            @Override
-+            public void onClick(View v) {
-+                MainActivity.this.openVivoBackgroundPermission();
-+            }
-+        });
-+        root.addView(vivoBgButton);
-+        TextView vivoHint = new TextView(this);
-+        vivoHint.setText("点开后找到\"允许高耗电\"或\"自启动\"，手动打开。不同 OriginOS 版本页面入口不完全一样，"
-+                + "如果跳转的页面不太对，就在里面自己翻找一下同类设置；实在跳不过去会退到应用详情页。");
-+        vivoHint.setTextSize(11.0f);
-+        vivoHint.setTextColor(Color.rgb(161, 136, 127));
-+        vivoHint.setPadding(0, 0, 0, 8);
-+        root.addView(vivoHint);
-         root.addView(divider());
- 
-         TextView modeTitle = new TextView(this);
-@@ -485,6 +532,63 @@ public class MainActivity extends Activity {
-         }
-     }
- 
-+    /** 标准 Android 电池优化白名单弹窗：多数机型（包括大部分 vivo）都认，点一下就是系统级授权确认框 */
-+    private void requestIgnoreBatteryOptimizations() {
-+        try {
-+            PowerManager pm = (PowerManager) getSystemService(POWER_SERVICE);
-+            String pkg = getPackageName();
-+            if (pm != null && pm.isIgnoringBatteryOptimizations(pkg)) {
-+                Toast.makeText(this, "已经在电池优化白名单里了，不用重复设置", Toast.LENGTH_SHORT).show();
-+                return;
-+            }
-+            Intent intent = new Intent(Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS);
-+            intent.setData(Uri.parse("package:" + pkg));
-+            startActivity(intent);
-+        } catch (Exception e) {
-+            // 部分定制系统屏蔽了这个标准 Intent，退到系统电池设置页让用户自己找
-+            try {
-+                startActivity(new Intent(Settings.ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS));
-+            } catch (Exception e2) {
-+                Toast.makeText(this, "无法打开电池优化设置: " + e2.getMessage(), Toast.LENGTH_SHORT).show();
-+            }
-+        }
-+    }
-+
-+    /**
-+     * vivo/iQOO OriginOS 的"后台高耗电"权限页是厂商私有页面，没有标准 Android API，
-+     * 只能按已知的组件名尝试跳转，不同系统版本入口可能不一样，所以做了一串按顺序尝试的候选列表，
-+     * 全部失败就退到本应用的系统详情页，让用户自己在里面找权限管理入口。
-+     */
-+    private void openVivoBackgroundPermission() {
-+        String[][] candidates = {
-+                {"com.vivo.permissionmanager", "com.vivo.permissionmanager.activity.BgStartUpManagerActivity"},
-+                {"com.vivo.permissionmanager", "com.vivo.permissionmanager.activity.PurviewTabActivity"},
-+                {"com.iqoo.secure", "com.iqoo.secure.ui.phoneoptimize.AddWhiteListActivity"},
-+                {"com.iqoo.secure", "com.iqoo.secure.ui.phoneoptimize.BgStartUpManagerActivity"},
-+                {"com.vivo.abe", "com.vivo.applicationbook.ui.BatteryOptimizeManagerActivity"},
-+        };
-+        for (String[] c : candidates) {
-+            try {
-+                Intent intent = new Intent();
-+                intent.setClassName(c[0], c[1]);
-+                intent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
-+                startActivity(intent);
-+                return;
-+            } catch (Exception ignored) {
-+                // 这个候选在当前系统上不存在，试下一个
-+            }
-+        }
-+        // 都不行，退到应用详情页，用户自己在里面找"权限"或"自启动"相关入口
-+        try {
-+            Intent intent = new Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS);
-+            intent.setData(Uri.parse("package:" + getPackageName()));
-+            startActivity(intent);
-+            Toast.makeText(this, "没找到专用入口，已跳转到应用详情页，自己找一下权限管理", Toast.LENGTH_LONG).show();
-+        } catch (Exception e) {
-+            Toast.makeText(this, "跳转失败: " + e.getMessage(), Toast.LENGTH_SHORT).show();
-+        }
-+    }
-+
-     /** 查询桌面图标（LauncherAlias 组件）当前是否可见 */
-     private boolean isLauncherIconVisible() {
-         try {
